[project]
name = "agent-dev"
version = "0.1.0"
description = "Stateless multimodal chat API built on the OpenAI Responses API"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.116.1",
    "openai>=2.28.0",
    "openai-agents>=0.12.3",
    "openviking>=0.2.6",
    "python-dotenv>=1.2.2",
    "uvicorn>=0.35.0",
]

